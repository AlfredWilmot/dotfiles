# dotfiles!

Clone repo then make a soft symbolic link to wherever the service that uses the dotfile is expecting to find it.
Add missing packages ... (TODO)

> Vim:
> `ln -sf ${PWD}/.vimrc ~/.vimrc`
